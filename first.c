/* Program to accept 2 integers from the keyboard as input, calculate 
and print their sum */
#include <stdio.h>
void main()
{
     int a, b;
     scanf("%d%d", &a, &b);
     printf("a = %d, b = %d", a, b);
}